<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LabelStyle" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/aliceblue</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="SolidButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/aliceblue</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="EditTextStyleLogin">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:backgroundTint">@color/ghostwhite</item>
        <item name="android:textColor">@color/aliceblue</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColorHint">@color/ghostwhite</item>
        <item name="android:paddingBottom">16dp</item>
    </style>

</resources>
